# Run: fresheyes-result-handle-binding

Instruction: Fix jibot-code#we92 in /Users/joi/repos/.worktrees/we92-reviewer-cannot-re-emit-other-reviews (fork of danshapiro/fresheyes): a fresheyes review can re-emit an earlier review it found on disk as its own findings (upstream issue #11). Reproduce with a failing test using a fake provider binary, then fix at the smallest point the evidence supports — the result must carry the run handle and fresheyes-progress.sh --result must refuse a result whose handle is not the run's, as a named failure state with non-zero exit, documented in SKILL.md. Both providers (gpt/codex and claude), manual + detached + automatic launches. Start at medium rung (never light — an independent-model fresheyes pass is required). Brief: TASK-jibot-code#we92.md in the worktree root.

Stage: code-review:fix
Rung: heavy (start-floor heavy: hard-trigger "a contract/API consumed outside this repo changes shape" — `fresheyes-progress.sh --result` is the fleet's review-gate CLI and gains a refusal state; BR2 REV2 NOV1 INT2 FC2 = 9). Dispatch brief floored it at medium; the evaluator floored it higher, and the ratchet never drops.
Lens (mandatory focus in EVERY review pass, every stage): "must not make jibot-code#f1vd (verdict-marker binding) harder"; "must not redo the OS sandbox work (jibot-code#g0hr)".
Round cap: the dispatch brief's THREE rounds per phase (spec, plan, code) outranks heavy's 6-pass budget. Floored-at-heavy-by-hard-trigger: yes — the judge may not STOP before pass 3.
Spec: docs/superpowers/specs/2026-09-22-fresheyes-result-handle-binding-design.md
Plan: docs/superpowers/plans/2026-09-22-fresheyes-result-handle-binding.md
Agency project: —
Kata: jibot-code#we92
Fresheyes launches: 8/8 (budget spent; any further independent pass goes down the fallback ladder)
Last reviewed: spec 76a9cf1 (r1) → b3d7a6f (r2) → d8ca21d (r3, subagent) | plan 2659fc5 (r1) → e1457fd (r2) → 3884c3e (r3, subagent) | code cd4b7df (r1) → 59b52ac (r2) → 67d44b8 (r3) → 80742a5 (final delta)
Pre-handoff review: — (repo is NOT repoman-managed; ending is docs/FLEET.md: upstream PR + --no-ff merge into feat/fleet-snapshot + tag)

Branch: fix/result-handle-binding, cut from upstream/main 5b174a7 (fork rule 2, docs/FLEET.md).
Reviews of this branch run `--gpt` with `FRESHEYES_GPT_MODEL=gpt-6-astra` (brief: security-shaped) from the INSTALLED plugin, never this worktree.

## Scorecards
Pass 1 [spec]: 0B/6S/2C/0R · fixed -/- · velocity = (—→8, escalation no) · judge: pre-floor
  fresheyes --gpt gpt-6-astra, snapshot 76a9cf1. Fixed 5 of 6 substantive: deferred review output before validation (both providers); terminal-state preservation (EXIT trap, heartbeat guard, automatic non-zero branch); automatic result discoverability (`result_path` + handle-named automatic JSON); the absent-marker claim narrowed and pinned by a test; the macOS gate scoped to what passes on unmodified upstream/main. Rebutted 1: the snapshot commit's message is not a defect — it is a dangling `commit-tree` snapshot that never lands. Both cosmetics fixed (status.json does not carry the handle today; baseline vs fleet-hardened Claude launch stated separately).
Pass 2 [spec]: 3B/0S/0C/1R · fixed 5/6 prior · velocity ↓ (8→4, escalation no) · judge: CONTINUE
  fresheyes --gpt gpt-6-astra, delta 76a9cf1→b3d7a6f, lens + standing rebuttal carried. Judge CONTINUE by the floor rule (floored at heavy, pass<3). All three blockers fixed: (1) validate before EVERY sidecar flush — under `set -euo pipefail` a provider can emit a good result then exit non-zero, so the failure branch would have printed an unvalidated review; (3) one resolver for verification AND delivery, so `--result` cannot return a transcript nothing checked; (4) automatic mode requires `run_handle` in the runner, because the Claude parser's JSON-text fallback never sees the schema. Deferred 1 (judge concurred): the `touch_heartbeat` read-modify-write race, pre-existing and unwidened — follow-up issue.
Pass 3 [spec]: 2B/8S/3C/0R · fixed 4/4 prior · velocity ↑ (4→13, escalation no) · judge: CONTINUE
  general-purpose subagent, FAILURE-MODES/ROLLBACK lens (heavy's pass-3 reviewer), delta b3d7a6f→d8ca21d, lens + 2 standing rebuttals carried. Both blockers fixed: the checker's undefined exit statuses would have killed the runner under `set -euo pipefail` and destroyed a successful review (now a fourth outcome, `error`/7, with every call site capturing status, manual failing open and automatic failing closed); and the plan contradicted the spec on the Claude flush order. Six substantives fixed: the poller expects the CALLER's handle, not the status file's; the resolver never falls back to the provider transcript and reads only the tail; `FRESHEYES_HANDLE` is gated, shape-validated and stripped from the reviewer's environment; the buffering sidecar is DELETED (the parser already writes the review to the log) — the design got smaller; the rollback claim is corrected and both mixed-version directions stated; the verdict now reads the resolver's file, so three selection sites become one; the acceptance criteria are reworded to what the tests can observe, and the mismatch diagnostic says the log holds the withheld text and must not be read back. S7 fixed in part (the marker line moves inside the Output template) and rebutted in part (no env var that disables the check — `git commit --no-verify` is the hook's existing escape hatch). All three cosmetics fixed.
  Phase closed at the dispatch brief's three-round cap with every BLOCKER and SUBSTANTIVE fixed or rebutted; the judge's CONTINUE cannot be acted on under that cap and no SPLIT signal was present. The mechanisms land in code, which the code phase reviews with its own three rounds.
  spec: passes 3 · elapsed 58 min
Pass 1 [plan]: 0B/12S/2C/0R · fixed -/- · velocity = (—→14, escalation no) · judge: pre-floor
  fresheyes --gpt gpt-6-astra, snapshot 2659fc5, lens + 3 standing rebuttals carried. All 12 fixed, 4 of them leak-class: automatic Claude's `is_error` branch prints the provider's text and returns 1 before the structured-output check ever runs; `--result`'s `print_failure_diagnostic` prints the last 20 lines of provider stderr verbatim, which on a replay is the refused review; treating every checker `error` as a mismatch contradicted manual fail-open (now: exit 7 = ran-but-could-not-read, any other status = could-not-run, which is what a missing helper actually returns); `RESULT_PATH` was set after the first `write_status`, so `set -u` would have killed every run; the runner's own `manual_verdict_from_log` was left out of the selector consolidation (the f1vd lens); env stripping covered only 2 of the 4 provider launches; the incomplete-record rule contradicted the expected-handle rule (spec corrected); the Claude provider test's exact-JSON assertion would have broken before its known macOS failure; neither checker-failure injection worked; the tests proved nothing about INDEPENDENT poller verification (now a runner-free fixture case, plus the inverse control); the assertion order could not produce RED-at-the-leak; the Linux RED recipe never transferred the new test. Both cosmetics fixed (stale line numbers re-derived with grep -n; "all seven" → every file in tests/).
Pass 2 [plan]: 5B/0S/0C/0R · fixed 14/14 prior · velocity ↓ (14→5, escalation no) · judge: CONTINUE (rule 2)
  fresheyes --gpt gpt-6-astra, delta 2659fc5→e1457fd, lens + 3 standing rebuttals carried. Judge verdict taken from the rubric's rule 2 without a dispatch: floored-at-heavy-by-hard-trigger = yes and pass 2 < 3 makes CONTINUE deterministic whatever the findings, so a dispatch could return nothing else — recorded here rather than spent. All five fixed: an unreadable recorded result still reached `print_failure_diagnostic`, which prints 20 lines of provider stderr verbatim (the refusal handing the text back through the diagnostic); deleting the checker did NOT block GPT automatic, because the automatic check was inlined in the runner's parse instead of going through the shared checker — now it goes through the checker, which is both the one-home property and what makes automatic fail closed; the exit contract contradicted itself on argv errors (7 is now reserved for "ran, could not read"; a usage error is 2 and joins "could not run"); a Claude automatic `is_error` mismatch could not report the foreign handle because the parser returns before the JSON result exists, so the terminal write now records `result_handle`; and the stderr-leak regression seeded the PRIOR run's stderr while the diagnostic reads the POLLED run's, so it could have passed with the unsafe diagnostic still in place.
Pass 3 [plan]: 0B/7S/2C/0R · fixed 5/5 prior · velocity ↑ (5→9, escalation no) · judge: CONTINUE (rule 5) — phase closed at the brief's 3-round cap with ZERO blockers, which is the brief's convergence test
  general-purpose subagent, IMPLEMENTABILITY lens, delta e1457fd→3884c3e, lens + 4 standing rebuttals carried; it verified all five round-2 fixes against the code and confirmed four genuinely closed. Seven substantives fixed: Q1 was closed at only one of the TWO `print_failure_diagnostic` call sites, and its `last_error=` line still prints a stderr line before the tail the fix patched (suppression is now a parameter applied at both sites, and `last_error` may not come from stderr there); the poller's state machine had no arm for `error`/7, which is the exact status a deleted `result_path` produces; the legacy output mode had no `handle_mismatch` branch and would report a terminal refusal as a live review; `write_status`'s signature contradicted the new `result_handle` field; refusing on `absent` before the Claude automatic failure branch would have reported every real API error, rate limit and auth failure as a handle failure and discarded the only diagnostic that says why (now only a `mismatch` preempts that branch); the leak assertions covered stdout but not stderr, which both Claude failure branches write to; and 1.2 seeded no prior AUTOMATIC artifact although 1.3 and 1.10(a) replay one. Both cosmetics fixed (line numbers re-verified line by line; `handle_mismatch` gets `severity:"error"`, not `"info"`).
  plan: passes 3 · elapsed 45 min
Pass 1 [code:fix]: 5B/5S/7C/0R · fixed -/- · velocity = (—→17, escalation no) · judge: CONTINUE (rule 2)
  Stage 1 (general-purpose subagent, which RAN the code) ∥ Stage 2 (fresheyes --gpt gpt-6-astra), both on cd4b7df, lens + 3 standing rebuttals carried. Judge by rubric rule 2 (floored at heavy, pass<3): CONTINUE regardless of findings, so no dispatch.
  Stage 2, four leak paths, all on error branches: a refusal whose terminal status write failed left the record at `running`, so the poller said `died` and its diagnostic quoted the provider's stderr — the refused review; an EMPTY recorded result reads as `absent`, not unreadable, so keying the suppression on the checker's exit code missed it; automatic mode's provider-failure branch printed the unchecked result and its stderr when the checker was unavailable; and a refused result kept its verdict, because the poller cleared the one it read and then repopulated it from status.json. Fixed in ca61a65.
  Stage 1, one BLOCKER it reproduced by running the poller: `result_path` from status.json was an unvalidated path that `--result` cat'd, so a record naming any readable file turned the poller into a read primitive — in a world-writable default log dir. Plus: polling with a handle SUFFIX (which the resolver's own glob permits) accused a correct review of being another run's; the refusal message read "carries review run another review run" when no foreign marker existed; the diagnostic named the transcript while the JSON result held the withheld text; and `set +e` … `set -e` ENABLED errexit in a script that never had it — my round-1 fix had made that worse by moving the block earlier. Fixed in 59b52ac. Cosmetics fixed: a dead assertion in the claude provider test (its call site passed one argument), exit-6 assertions where the contract is 6, a header note that a base run proves only the first case, a length-bounded marker token, and the self-review note in SKILL.md.
  Non-vacuity checked by running the new cases against the PRE-fix commits: the died-after-refusal case, the refused-verdict case and the result_path-escape case each fail there and pass here.
  code:fix: passes 1 · elapsed 31 min
Pass 2 [code:fix]: 2B/7S/4C/0R · fixed 17/17 prior · velocity ↓ (17→13, escalation no) · judge: CONTINUE (rule 5)
  Stage 1 (subagent, ran and mutation-tested the code) ∥ Stage 2 (fresheyes --gpt gpt-6-astra), delta cd4b7df→59b52ac, lens + 4 standing rebuttals carried. BOTH blockers sat in round 1's OWN fixes, which is the convergence signal: the path confinement was a string-prefix test over a path `-f` then followed, so `..` and an in-directory symlink walked out and `--result` printed the target; and taking the expectation from `status.json` let a repointed `.locator.<caller handle>` hand back another run's genuine review, verified against that run's own handle — no forgery, no marker rewriting, and the previous commit had caught it. Substantives: a missing recorded result fell back to the provider transcript; verification was gated on the metadata carrying a handle, so a record without one skipped it; `FRESHEYES_ALLOW_LEGACY_PROGRESS=1` turned the confinement off wholesale; the `died` message sent a reader to a log holding text that could not be tied to the run; and TWO test defects that made assertions meaningless — the claude provider comparison still did not execute (my previous commit's claim to have fixed it was wrong), and the empty-result case passed with its own fix reverted. Fixed in becdd1d and 67d44b8.
  The identity rule was corrected TWICE, so the second correction is the narrow one a reviewer prototyped and I verified: the polled handle, widened to the resolved file's name only when the polled handle is a SUFFIX of it — closer to reverting my change than extending it. PREPARED FALLBACK if round 3 finds a third generation in this same machinery: drop `result_path` from status.json entirely and keep the original file selection, which removes the confinement, transcript-fallback and identity questions at once.
  Non-vacuity, by running against the PRE-fix commit or by mutation: the traversal escape and the foreign-identity record leaked at 59b52ac; the locator repoint leaked at becdd1d; the rewritten empty-result case fails under the exact mutation the reviewer used to show the old one was inert.
  code:fix: passes 2 · elapsed 68 min
Pass 3 [code:fix]: 0B/5S/0C/0R · fixed 13/13 prior · velocity ↓ (13→5, escalation no) · judge: SCOPE CUT (the brief's revert-instead-of-extend remedy, recommended by the reviewer)
  fresheyes --gpt gpt-6-astra, delta 59b52ac→67d44b8, lens + 4 standing rebuttals carried, and the round's prompt asked explicitly whether to CUT rather than fix again. It answered: "This is a third generation of defects in the same resolver/identity machinery. I recommend the prepared scope cut." Its five findings: the suffix widening accepted a bare string suffix while the glob it mirrors requires a hyphen, so `.locator.1234` repointed at fresheyes-...-ab1234.log delivered that run's review; stderr suppression was still gated on the record carrying a handle, so a record without one in state=failed quoted the refused review from stderr through the `died` diagnostic; a resolver rejection never reached the JSON status, which still said complete/verdict/result_available; the containment allowlist omitted the compatibility directory `_find_base_for_pid` itself searches under FRESHEYES_ALLOW_LEGACY_PROGRESS; and SKILL.md's Step 5 table, declared authoritative, still told the agent to inspect the `died` log that the Step 6 bullet says not to read.
  TAKEN, in 330339c: `status.json` no longer records the result path at all. Three of the five findings are structurally gone with it (containment, resolver-rejection reporting, the compatibility directory), and so are the two the earlier rounds spent on the same machinery. The other three are fixed in retained code. The cost is written into the spec rather than hidden: the poller cannot verify an automatic result, whose own runner is the gate.
  code:fix: passes 3 · elapsed 96 min
Pass 3 [code:fix] Stage 1: 0B/3S/6C/0R · judge: PASSED — and it DISAGREED with Stage 2 about the cut
  general-purpose subagent on the same commit 67d44b8: a pristine export, the full suite, 7 adversarial probes, a 7-cell end-to-end matrix (both providers × both modes × foreground/detached) through all three poller output modes, and 8 MUTATIONS of the fix hunks. Every new test case died under its own mutation — none is vacuous. It could not break the corrected machinery and answered the cut question with "No — ship this, do not cut back", on the grounds that the cut loses the poller's ability to verify an automatic result and does not fix the two older defects that remain.
  Its three substantives: the widening was unanchored and its test never went through the glob it cites; the legacy glob silently takes `ls -t | head -1` when two bases share a pid suffix; and the TRACKER confinement is still the prefix+`-f` test round 2 rejected for the result path — a repointed `.locator` escapes by `..` or a symlink and `--result` prints the target. It verified the last two byte-identical on upstream/main and on fleet-2026.09.21.3, so neither belongs to this branch.
  HOW THE DISAGREEMENT WAS RESOLVED: the cut stands — smaller to offer upstream, three finding classes removed rather than guarded, and the lost property is held by the runner at the gate that matters — but Stage 1 was right about the regression it introduces, so automatic records are now excluded from the poller's check outright (their result is a JSON file the poller cannot name, and reading the transcript instead would refuse good runs for text they merely quoted). The widening is anchored to the numeric-pid glob and its test rewritten to resolve through the glob. Cosmetics taken: both streams asserted in the result-path case, the `died` wording asserted, and SKILL.md scoped to say the binding needs a handle.
  FOLLOW-UP FILED: jibot-code#w7ts — the pre-existing tracker escape and the ambiguous-glob pick.
Final delta [code:fix]: 2B/0S/0C/0R · judge: n/a (post-round delta) — fresheyes --gpt gpt-6-astra, 67d44b8→80742a5, launch 8/8
  BOTH blockers were mine, from the automatic exemption added in that same delta: skipping the check for automatic records skipped the identity comparison and the stderr suppression with it, so a `.locator` repointed at a completed automatic run delivered THAT run's result under the caller's handle (for the Claude provider the review log IS the structured JSON, run_handle included), and a run whose checker had been unavailable had its withheld provider stderr handed back through the poller's `died` diagnostic. The exemption is REMOVED in 180072e: a GPT transcript carries no marker line, so it reports unverified instead of refusing — the behaviour the exemption was meant to buy — and a transcript that does carry a foreign marker is a replay signal worth refusing. Both paths now have regressions, both confirmed to leak against 80742a5. The spec's account of the cut's cost was wrong in the same direction and is corrected.
  The 8-launch budget is now spent, so the pass over 180072e goes down the fallback ladder: roborev with an explicit other-family `--agent codex`.

## Chunks
- [x] repro — the failing test; RED at the leak on macOS AND Linux on unmodified upstream/main (`/tmp/claude-501/red-mac.txt`, `/tmp/claude-501/red-linux.txt`)
- [x] fix — run-marker binding in the prompts, the runner and the poller + SKILL.md. Committed as cd4b7df together with the test: a commit that is red on its own has no value to the upstream PR, and the RED evidence is the captured output above, not a red commit.
- [ ] integration — merge into feat/fleet-snapshot, tag, upstream PR

## Test results (chunk 2, commit cd4b7df)
- Linux (Lima fe-test, Ubuntu 24.04): all SEVEN files rc=0, including the new one.
- macOS (macct): gpt-provider, prompt-contract, verdict, handle-binding rc=0; claude-provider, detach, progress rc=1 at EXACTLY their baseline assertions (`ps -o sess=`, bash 3.2 `$` escaping, BSD `wc -l` padding). Baseline recorded in Notes.
- Two real bugs the tests caught during the build, both invisible without them: a `handle` name in the `write_status` heredoc was rebound by the record-reading `with open(...) as handle` block, so every terminal write after the first raised and was swallowed by `|| true`; and `verify_result_handle` restored `errexit` before returning 6, killing the poller on the very path it was added for.

## Notes
- Spec pass 1 was launched before the start-floor evaluator returned, so its prompt does not carry the Lens. Every later pass carries it verbatim.
- FOLLOW-UP FILED: jibot-code#wf8w — `touch_heartbeat`'s read-modify-write can resurrect a stale status over a terminal state (the round-2 deferred finding).
- BASELINE measured on unmodified `upstream/main` 5b174a7, 2026-09-22, env with the five `FRESHEYES_*` vars unset (docs/FLEET.md § Tests):
  - macOS (macct): claude-provider rc=1 ("detached review shares the launcher's session (0)" — `ps -o sess=`), detach rc=1 ("scope with literal ${HOME}/$PATH must reach the provider intact" — bash 3.2), progress rc=1; gpt-provider, prompt-contract, verdict rc=0. Matches jibot-code#3scf; the fixes are on `feat/fleet-snapshot`, not on this base.
  - Linux (throwaway Lima `fe-test`, Ubuntu 24.04 aarch64, bash 5.2.21, python 3.12.3): all six rc=0.
  - So the fix branch's macOS gate is "the three that pass still pass, the three that fail fail at the same assertion"; all seven green on macOS is the integration merge's acceptance.
